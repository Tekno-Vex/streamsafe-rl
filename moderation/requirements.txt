# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# Async Performance
uvloop>=0.21.0

# Redis Client
redis>=5.2.0

# Message Queue (choose one or both)
kafka-python>=2.0.2
# confluent-kafka>=2.3.0  # Alternative Kafka client (uncomment if preferred)

# Data Format for Logging
pyarrow>=18.0.0

# Optional: Schema validation utilities
jsonschema>=4.23.0

# Testing (optional for now, but useful)
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0  # For testing FastAPI endpoints